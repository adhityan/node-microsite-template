apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gc-chat.name" . }}-test-connection"
  namespace: {{.Release.Namespace}}
  labels:
    {{- include "gc-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gc-chat.name" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
