apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "corona-backend.name" . }}-test-connection"
  namespace: {{.Release.Namespace}}
  labels:
    {{- include "corona-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "corona-backend.name" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
