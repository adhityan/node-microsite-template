apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ATEMPLATE.name" . }}-test-connection"
  namespace: {{.Release.Namespace}}
  labels:
    {{- include "ATEMPLATE.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ATEMPLATE.name" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
