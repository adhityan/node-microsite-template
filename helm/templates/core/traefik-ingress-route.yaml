{{- if .Values.traefik.crds -}}
{{- range $host := .Values.ingress.hosts }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "ATEMPLATE.name" $ }}-ingress-route-https
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "ATEMPLATE.labels" $ | nindent 4 }}
  annotations:
    helm.sh/hook: "post-install"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ $host }}`) && PathPrefix(`/v{{ include "ATEMPLATE.version" $ }}/`)
      kind: Rule
      middlewares:
        - name: {{ include "ATEMPLATE.name" $ }}-strip-version-prefix-middleware
      services:
        - name: {{ include "ATEMPLATE.name" $ }}-service
          namespace: {{ $.Release.Namespace }}
          port: 80
  tls:
    secretName: {{ include "ATEMPLATE.name" $ }}-{{ $host | replace "." "-" }}-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "ATEMPLATE.name" $ }}-ingress-route-http
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "ATEMPLATE.labels" $ | nindent 4 }}
  annotations:
    helm.sh/hook: "post-install"
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ $host }}`) && PathPrefix(`/v{{ include "ATEMPLATE.version" $ }}/`)
      kind: Rule
      middlewares:
        - name: {{ include "ATEMPLATE.name" $ }}-https-only-middleware
      services:
        - name: {{ include "ATEMPLATE.name" $ }}-service
          namespace: {{ $.Release.Namespace }}
          port: 80
{{- end }}
{{- end }}