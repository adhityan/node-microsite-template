{{- if .Values.traefik.crds -}}
{{- range $host := .Values.ingress.hosts }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "gc-chat.name" $ }}-ingress-route-https
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "gc-chat.labels" $ | nindent 4 }}
  annotations:
    helm.sh/hook: "post-install"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ $host }}`) && PathPrefix(`/v{{ include "gc-chat.version" $ }}/`)
      kind: Rule
      middlewares:
        - name: {{ include "gc-chat.name" $ }}-strip-version-prefix-middleware
      services:
        - name: {{ include "gc-chat.name" $ }}-service
          namespace: {{ $.Release.Namespace }}
          port: 80
  tls:
    secretName: {{ include "gc-chat.name" $ }}-{{ $host | replace "." "-" }}-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "gc-chat.name" $ }}-ingress-route-http
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "gc-chat.labels" $ | nindent 4 }}
  annotations:
    helm.sh/hook: "post-install"
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ $host }}`) && PathPrefix(`/v{{ include "gc-chat.version" $ }}/`)
      kind: Rule
      middlewares:
        - name: {{ include "gc-chat.name" $ }}-https-only-middleware
      services:
        - name: {{ include "gc-chat.name" $ }}-service
          namespace: {{ $.Release.Namespace }}
          port: 80
{{- end }}
{{- end }}